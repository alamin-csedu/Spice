@model IEnumerable<Spice.Models.Coupon>

@{
    ViewData["Title"] = "Index";
}

<div class="container border m-5 background-white">
    <div class="row m-4 p-">
        <div class="col-10">
            <h1>Coupon List</h1>
        </div>
        <div class="col-2 align-middle">
            <a class="btn btn-primary" asp-controller="Coupon" asp-action="Create"><i class="fas fa-plus"></i> &nbsp; Create New</a>

        </div>
    </div>

    <div class="border m-4">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CouponType)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Discount)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.MinimumAmount)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.isActive)
                    </th>
                    <th></th>

                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CouponType)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Discount)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.MinimumAmount)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.isActive)
                    </td>
                    <td class="text-right">
                        <a class="btn btn-primary" asp-area="Admin" asp-controller="Coupon" asp-action="Edit" asp-route-id="@item.Id"><i class="fas fa-edit"></i></a>
                        <a class="btn btn-secondary" asp-area="Admin" asp-controller="Coupon" asp-action="Details" asp-route-id="@item.Id"><i class="fas fa-list"></i></a>
                        <a class="btn btn-danger" asp-area="Admin" asp-controller="Coupon" asp-action="Delete" asp-route-id="@item.Id"><i class="fas fa-trash-alt"></i></a>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>
</div>
